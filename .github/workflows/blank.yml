name: React Frontend CI

on:
  push:
    branches:
      - MAIN

jobs:
  build:
    runs-on: self-hosted

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2
    steps:
    - name: Checkout Test
      run: echo "Test" > test
